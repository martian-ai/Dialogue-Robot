# sort 和 sorted https://www.runoob.com/python/python-func-sorted.html
# sort 处理list， 返回null
# sorted 处理迭代器，返回排序后的数据结构


    # input_lines = sorted(list(enumerate(input_lines)), key=lambda x: -len(x[1]))
    # 按照长度排序
